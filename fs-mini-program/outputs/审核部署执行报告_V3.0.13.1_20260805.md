# 风声 V3.0.13.1 —— 全面测试 + 送审 + 部署执行报告

> 日期：2026-08-05
> 执行人：小豆子
> 版本：V3.0.13.1（commit `3e228bf` 主体 + `5845481` 客户可见页诚实修正，分支 `feat/mp-v3-curation-prep-20260801`）
> 结论：**全面测试通过（8 项微信合规铁检全过 + 6 轮 UX 走查 0 报错），已提交送审工单触发内部审核，部署包就绪（真机上传需人工 GUI）**

---

## 一、全面测试结果（mp-weixin 目标）

### 1.1 构建
- `npm run build:mp-weixin` DONE；主包 **1.7M** < 2MB；分包 `package-curation` **84K** < 2MB。
- `lazyCodeLoading` 注入、`libVersion` 锁 `2.32.3`、版本号唯一 `3.0.13.1`（无硬编码漂移）。

### 1.2 8 项微信官方审核合规自检（全部通过）

| # | 检查项 | 结果 |
|---|---|---|
| 1 | 虚拟支付（会员/VIP/充值/订阅/兑换/¥价格/升级为企业） | ✅ UI 层 0 命中；仅 `entries_slim.js` 内容含"会员费"（黑中介警示真实内容，人工排除） |
| 2 | 诱导分享（share 得积分/红包/会员） | ✅ `incShare` 仅定义未接线；`earnTasks` 无 share/积分/红包 |
| 3 | 绝对化用语（最/第一/国家级/顶级/最佳/唯一/首选） | ✅ 无营销超级词；内容内"满五唯一/唯一住房"为真实房产术语，人工排除 |
| 4 | 功能完整性占位信号（模拟/测试版/开发中） | ✅ 0 命中 |
| 5 | 资源单文件 ≤200K | ✅ 0 超（含 static 全部图片/音频） |
| 6 | wxss 禁用中文类 / wxml 禁标签（small/br/ul/li…） | ✅ 0 命中 |
| 7 | 主包/分包 < 2MB | ✅ 1.7M / 84K |
| 8 | 后端依赖（login 未强制启动期） | ✅ `onLaunch` 仅本地 `markDone('login')`，核心功能纯本地，无白屏风险 |

### 1.3 功能走查（用户视角 6 轮，H5+Playwright 真渲染）
- 0 `PAGEERROR`、0 `console.error`（功能层面；埋点 CORS 已修，重跑 0 报错）。
- 覆盖：购房改善/租住起步/业主出售/房东出租/购房教育/懒人默认。
- 各场景命中数（上一轮同脚本确证）：购房改善 5/4/5/4（40 强/34 法源）、业主出售 5/4/5/3（40/35）、租住起步 5/3/5/4（40/27）、购房教育 5/4/5/2（8/8）、懒人默认 5/4/5/2（11/10）。
- 导出工具（客户可见页/HTML/豆包/摘要/存入认知卡）全通；无客户存档时"存入认知卡"诚实 toast 不假存。
- **已知诚实缺口（不影响送审）**：房东出租线 R4 走查 0 命中（slim landlord 数据 0），页面诚实提示"暂无匹配词条"；dataRef/caseRef 覆盖率 0%，客户页降级为版式示意。

---

## 二、送审（已触发）

- **工单已提交 main 并推送** → 触发内部审核机器人（外部服务盯 `工单/小酒窝儿/`）。
  - commits `0736183`（工单）→ `f8b167b`（补客户可见页诚实修正更正记录）on `main` → `origin/main`。
  - 工单文件：`工单/小酒窝儿/审核_风声V3.0.13.1_客户可见页+全场景策展引擎_20260805.md`
- **代码 PR #211 已更新**：特性分支推送 `4cc6188 → 3e228bf`（主体）+ `5845481`（客户可见页诚实修正）。
- 内部审核结论将通过 Issue 评论同步（P0 2h 内 9 项铁检）。

---

## 三、部署（包就绪，真机上传需人工 GUI）

> ⚠️ 按项目铁律，AI 沙箱只能「构建+打包+present」，无法替代微信开发者工具的真机 GUI 上传。以下为必须人工完成的步骤。

### 3.1 部署包
- `fs-mini-program/outputs/mp_weixin_v3.0.13.1_20260805.zip`（801K，mp-weixin 构建产物）

### 3.2 真机上传 + 提交审核（人工）
1. 打开**微信开发者工具** → 导入 `dist/build/mp-weixin` → 点「上传」生成开发版（需 GUI 点「开启服务端口」+ 上传按钮，沙箱无法代点）。
2. 登录 **MP 后台** → 该小程序 →「版本管理」→ 将开发版**设为体验版**（生成体验码）。
3. 「提交审核」→ 填写类目/标签 → 等待微信审核（1-7 天）。
4. **MP 后台人工配置项（代码无法代配）**：
   - 「隐私保护指引」+ 我的页「隐私政策」入口；
   - 「房产中介」类目资质、小程序 ICP 备案、企业主体认证。

### 3.3 催部署路径（若需我接力）
- 拉飞书群 `oc_68714b732f740d284e51268ba1b65614` @小扣子/小酒窝儿，请求「上传体验版 + 重新生成体验码 + 提交审核」并附 zip 路径；
- 或用户本人在开发者工具点上传（最快，AI 接力其余）。

---

## 四、送审前拦截并修复的关键问题（诚实修正）

提交送审前复核发现 `curate-client`（客户可见页）两处**真实虚标**（iron rule 0 容错），已在 `5845481` 修复：
1. 硬编码「法源 100% 真实」（实测 slim 614/634＝96.8%，含 20 条占位「待补充法源」）→ 改「法源真实标注」，不再写死数字。
2. 页面 5 处泄露内部同事名「小眼镜」（如「待小眼镜补入字典真实数据」）→ 改「真实数据补入字典后自动替换」，客户页不再出现内部称谓。

修复后本工单「数据诚实铁律落地」表述方为真。另：`tracker.js` 加 H5 本地跳过跨域埋点，消除控制台 CORS 刷屏（重跑 0 报错）。

---

## 五、送审后 fast-follow（非阻断）
1. **P0 slim union 768**：合并 main(704, landlord25) ∪ 分支(634, tenant44) 修房东出租线 0 命中。
2. **P1 客户页占位隐藏**：dataRef/caseRef 0% 时隐藏渲染而非显示"待补充"。
3. **P1 dataRef/caseRef 真实数据**：按已圈定场景清单补（待小眼镜排产）。

---

## 六、交付物清单
- 部署包：`fs-mini-program/outputs/mp_weixin_v3.0.13.1_20260805.zip`
- 送审工单：`工单/小酒窝儿/审核_风声V3.0.13.1_客户可见页+全场景策展引擎_20260805.md`（已推 main）
- 功能走查报告+截图：`fs-mini-program/outputs/ux_walk_v3.0.13.1_20260805/`
- H5 构建验收：`fs-mini-program/outputs/verify_v3.0.13.1_20260805/`
