# V3.0.7.3 根治版本号硬编码 bug

## 根因（用户 19:15 截图关键发现）
用户发"我的"页底部截图，显示：
- 「风声 v3.0.7」+ 下方红字「若此版本号不是 3.0.7，说明手机仍是旧包，请重新上传最新 zip」

**真相**：
1. 用户手机跑的是 V3.0.7 这个最早期版本（commit 0ef4ab7），**不是 V3.0.7.1 / V3.0.7.2**——之前所有修复（chip 70vh + 客户档案诊断条）全没到手机
2. 那段红字不是部署者加的，是我**自己**在 V3.0.7 时代的 `src/pages/profile/index.vue:40` 写的 ver-tip 防呆提示
3. 截图暴露了**升级时的硬编码漏改 bug**——我升级 3.0.7.1 / 3.0.7.2 时只改了 `share.js` 里的 APP_VERSION，**漏改两处 wxml 里的硬编码版本号**

## 漏改的两处硬编码
- `src/pages/profile/index.vue:40` 旧：`若此版本号不是 3.0.7，说明手机仍是旧包`
- `src/pages/clients/index.vue:11` 旧：`包版本 v3.0.7.2`

两处都改成 `{{ appVersion }}` 模板插值，从 share.js 单一真源走——以后改版本号再不会漏。

## 送审铁律（6 项全过）
- ver-tip 动态化 ✓
- 客户档案诊断条 动态化 ✓
- >200K 资源 0
- 虚拟支付泄漏（¥/VIP/会员/订阅/充值/开通）0
- 跨包 require（主→分禁止）0
- 主包 1.6M < 2M ✓

## 送审包
- outputs/mp-weixin-v3.0.7.3-20260802.zip (768897 字节 / 92 文件)

## 部署闭环铁律（新增）
**反思**：V3.0.7.1 (commit 2729b52) + V3.0.7.2 (commit 2921de8) 提交后只 present_files 给用户，**没主动拉群催部署**——这是用户反复"还是不行"的核心原因之一。

**新铁律**：每次 present_files 之后必须同步飞书群（oc_68714b732f740d284e51268ba1b65614）@小鱼儿/小扣子/小酒窝儿：
- 新版本号 + 修复了什么
- 附 outputs/zip 路径
- 明确请求「上传体验版 + 重新生成体验码」

## 用户操作（最简）
1. 用开发者工具导入 dist/build/mp-weixin
2. **上传为体验版**（不是开发版；开发版热重载不需要扫码）
3. 重新生成体验码 → 手机长按删除小程序 → 重扫体验码
4. 进"我的"页底部必须显示「风声 v3.0.7.3」+ ver-tip 跟着变「若此版本号不是 v3.0.7.3」
5. 进"客户档案" tab 第一眼看到黄底诊断条「包版本 v3.0.7.3 · 初始化 ✓ · 当前 N 张 · 存储 已seed/未seed」

**判断标准**（自检用）：
- 我的页 ver-tip 显示 v3.0.7.3 = 部署成功
- ver-tip 还显示 v3.0.7 = 部署失败（重传 zip）
- 客户档案没看到黄底诊断条 = 部署失败（重传 zip）
