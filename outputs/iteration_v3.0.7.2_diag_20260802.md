# V3.0.7.2 客户档案空白诊断 + 终极兜底

## 根因（用户 19:10 反馈）
- 截图特征：tabBar 顶部"客户档案" + 绿色 header（客户档案·一次委托·终生服务·"+ 新建"）
- 下半屏 100% 空白：既看不到 4 张示例客户卡片，也看不到空态引导"还没有客户档案"
- 这是一个**新症状** —— 之前空态/seed 兜底都已经在 V3.0.4 / 7.30 修过
- 用户报了 4 次"还是不行"，最怀疑手机仍跑旧版本；但截图里**根本没出现诊断条**说明新版本根本就没部署到手机

## 修复（commit V3.0.7.2）
### 1. 诊断条 - 自带版本号显示
- 在 section-header 下方加 `<view class="diag">`：包版本 v3.0.7.2 + 初始化 ✓/✗ + 当前客户数 + 存储 seed 状态
- 用户进客户档案页即一眼判断包版本，**不需要再发"我的"页版本号截图**
- 同时给"↻ 重试示例"按钮：点一下若仍 0 张，立即重写 4 张示例

### 2. onShow 终极兜底
- 在 initFromStorage 之后，若 clients 为 0：
  - 单独用 try/catch 读 rawClients 字符串
  - 若 raw 为空（key 完全不存在）→ 调 seedClients 兜底
  - 若 raw 存在但解析为空 → 尊重用户，不强塞（保持"空态真实可达"原则）
- `_triedAutoSeed` 标记避免重复试探

### 3. APP_VERSION 3.0.7.1 → 3.0.7.2
- share.js + profile 页底部"我的"页同步升级版本号

## 送审铁律（5 项全过）
- >200K 资源 0
- 虚拟支付泄漏（¥/VIP/会员/订阅/充值/开通）0
- 跨包 require（主→分禁止）0
- 主包 1.6M < 2M ✓
- 编译产物 grep 包版本 + 重试示例 ✓

## 送审包
- outputs/mp-weixin-v3.0.7.2-20260802.zip (768854 字节)

## 用户操作（最简路径）
1. 用开发者工具导入 dist/build/mp-weixin → 真机预览
2. **进客户档案 tab 第一眼**——应该看到黄底灰字条：**"包版本 v3.0.7.2 · 初始化 ✓ · 当前 0 张 · 存储 未seed"**
3. 如果这条出现 → 证明是新包（问题确实在 storage/seed），按"↻ 重试示例"即可恢复
4. 如果这条没出现 → 你手机跑的还是老包，必须**长按删除小程序重扫体验码**
