# 送审工单 · 客户档案 V3.0.2（删除自定义弹窗 + 清空示例 + onShow 竞态修复）

> **状态**：🔴 已被 V3.0.3 根治修复替代（见 `审核_客户档案V3.0.3_20260802.md`）
> **送审人**：小豆子
> **审核人**：小酒窝儿
> **日期**：2026-08-02
> **关联PR**：#211
> **优先级**：P0 紧急（由 V3.0.3 接管）
> **说明**：本工单为客户档案 V3.0.2 增量送审，覆盖删除改自定义弹窗、清空示例、示例灰化、onShow seed 竞态修复；新提交自动触发分级审核

---

## 一、送审范围

微信小程序客户档案模块（tabBar 第 4 项），V3.0.2 增量：

| 改动 | 类型 | 说明 |
|------|------|------|
| 删除客户改自定义确认弹窗 | 修复 / 体验 | 不再依赖 `uni.showModal`，规避 uni Web 版 showModal 缺陷（`Cannot set properties of null (setting 'scrollTop')`），H5 预览 / WeChat 真机一致可用 |
| 「清空示例」一键按钮 | 新增 | 仅删 `seed:true` 示例客户，真实客户零影响；复用同一自定义弹窗二次确认，文案动态显示示例张数 |
| 示例客户灰化 + 「示例」角标 | 体验 | 虚线框 + 60% 透灰 + 灰头像 + 名字旁「示例」角标，详情副栏追加「· 示例客户·仅供参考」灰字，明确参考与真实客户区分 |
| 修复 onShow seed 竞态 | 修复（真 bug） | 修复「清空示例 / reload 后示例回弹」；store 新增 `_initialized` 标记，`initFromStorage` 末尾置位，页面守卫加闸门 |

## 二、改动文件

- `fs-mini-program/src/pages/clients/index.vue`（自定义 modal 模板 + 清空示例按钮 + 灰化样式 + askDel/askClear/confirmOk/confirmCancel 方法 + `hasSamples` computed + `_initialized` 守卫）
- `fs-mini-program/src/store/user.js`（`_initialized` state 标记 + `clearSamples()` action）

## 三、验证（真渲染为证据）

- H5 真渲染（Playwright + Chromium）**11/11 通过**：seed / 灰化 / 新建真实客户 / 编辑 / 自定义删除弹窗可点通 / 清空示例 / 持久化 / reload 不回弹 / 空态大卡片；业务控制台 0 错误
- store 逻辑 Node 单测（Pinia + 内存桩）**14/14**：seed / 新建 / 编辑 / 定位 / 删除 / 清空示例
- mp-weixin 重建 `MP_EXIT=0`，资源 **0 个 >200K**，libVersion **2.32.3** 锁定
- 截图证据：`outputs/clients_fix/{list_with_samplebar, delete_custom_modal, clear_samples_modal}.png`

## 四、送审铁律自检（9 项铁检摘要）

| 项 | 结果 |
|----|------|
| 虚拟支付资质泄漏扫描（源码 `会员\|VIP\|¥[0-9]\|充值\|订阅\|开通\|升级为企业`） | 0 命中 ✅ |
| 图片 / 音频单文件 ≤200K | 0 触发 ✅ |
| tabBar 5 项（首页 / 知识 / 策展 / 客户档案 / 我的） | ✅ |
| wxss 中文类名 | 0 命中 ✅ |
| 主包 / 分包 <2MB | ✅ |
| mp-weixin + h5 构建 0 警告 | ✅ |
| 真渲染证据 | 见测试报告 + 截图 ✅ |

## 五、交付物

- **代码包（微信开发者工具导入用）**：`outputs/mp-weixin-v3.0.2-20260802.zip`（760KB / 88 文件）
- 变更清单：`outputs/iteration_changelog_v3.0.2_20260802.md`
- 测试报告：`outputs/clients_test_report_20260802.md`

## 六、下一步（人工）

- 小酒窝儿：真机预览 `dist/build/mp-weixin` → 上传 → 提交微信审核（P1 常规 12h 内完成铁检）
- 小鱼儿：确认合并 PR #211 到 main
- 审核结果与铁检通过状态通过 Issue 评论同步
