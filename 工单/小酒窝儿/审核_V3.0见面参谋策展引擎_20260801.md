# 送审工单 · V3.0 见面参谋·策展引擎

> **状态**：🔴 待审核（P0 紧急 · 2h 内 9 项铁检）
> **送审人**：风声交付流水线（Craft 自动送审，代表小鱼儿/总指挥）
> **审核人**：小酒窝儿
> **优先级**：P0（紧急）
> **日期**：2026-08-01
> **关联 PR**：#211（承载代码，分支 `feat/mp-v3-curation-prep-20260801`，最新 commit `49d0b96`）
> **结论同步**：分级审核结果经本 PR 的 Issue 评论同步
> **说明**：V3.0 见面参谋策展引擎 + 客户认知复利卡 + 客户档案「空白/建不了」真实 bug 修复

---

## 一、送审范围

1. **新增 · 见面参谋策展引擎**（`package-curation` 分包）：把真实业务字典转化为每次客户接触可执行的「说/带/问/跟」策展包，每项挂真实 `legalRef`，零编造。
2. **新增 · 客户认知复利卡**：客户偏好/决策信号随真实见面动作沉淀（非积分外壳）。
3. **修复 · 客户档案「空白/建不了」**（用户现场反馈的真实 bug）：空态大卡片化 + 一次性 seed 标记 + 遮罩层级 z-index 50→1000（解决 tabBar 盖住「创建/删除」按钮点击无响应）。

## 二、优化改进 + 新增功能清单

| 模块 | 改动点 | 解决啥 | 做了啥 | 验收 |
|------|--------|--------|--------|------|
| 策展引擎 | 新增 `package-curation/engine.js` | 见客户前不知聊啥/带啥/问啥 | 确定性（无 LLM）检索增强：双纵轴(购5/租4)+七维(4+2+1)+自由诉求 → 说/带/问/跟，每项挂真实 `legalRef` | ✅ |
| 策展准备页 | 新增 `package-curation/pages/curate-prep/index.vue`（分包非 tab 页） | 从零填表太重 | 客户档案一键带入预填 + 生成策展包 + 三段式时间轴 + 存入认知卡 | ✅ |
| 认知复利卡 | `src/pages/clients/index.vue` 新增卡片 | 客户偏好散落无积累 | 已知偏好/决策信号/关怀点，初始空、随真实动作沉淀 | ✅ |
| 客户档案修复 | `src/pages/clients/index.vue` + `src/store/user.js` | 空白且新建/删除无响应 | 空态大 CTA + `fs_seeded` 一次性标记 + 遮罩 `z-index:1000` | ✅ |
| 词典瘦身隔离 | `entries_slim.js`（333KB / 545 真实法源，移入分包） | 主包超 2MB 风险 | 主包降至 1.5M（<2MB 红线） | ✅ |
| 幻觉边界 R2 | engine.js | 编造依据 | 仅真实法源挂「依据」徽标，25 条如实标「依据整理中」 | ✅ |
| 用户视角铁律 | 产品面文案 | 操纵性用词 | 禁用「钩子/策略/转化」，统称「见后跟进/持续关怀」（grep 0 命中） | ✅ |

## 三、客户档案 bug 修复验证（真渲染 e2e）

- 冷启动 4 示例客户正常展示（不再完全空白）
- 真实新建客户（测试客户赵女士）→ 卡片数 4→5 增加
- 详情含「准备这次见面」→ 生成参谋 → 结果含「该说的 / 见后跟进」→ 存入认知卡 → 回看沉淀 ✅
- 删空后空态真实可达（一次性 seed 标记，不重塞示例）

## 四、P0 九项铁检（自测全绿 · 待小酒窝儿复核）

| # | 铁检项 | 结论 | 证据 |
|---|--------|------|------|
| 1 | 编译构建成功（mp-weixin + h5） | ✅ | `npm run build:mp-weixin` DONE；postBuildInject 注入 `lazyCodeLoading` + `libVersion 2.32.3` 生效 |
| 2 | 图片/音频单文件 ≤200K | ✅ | `find dist/build -size +204800c` 扫描 **空** |
| 3 | wxss 类名禁用中文 | ✅ | `grep -rP '[\x{4e00}-\x{9fff}]' *.wxss` 扫描 **空** |
| 4 | wxml 无 HTML 标签 / 无 v-html | ✅ | `<small>/<br>/<ul>/<li>/<i>/<b>/<span>/<label>/<h*>/<p>` + `v-html` 扫描 **空** |
| 5 | 虚拟支付资质泄漏 = 0 | ✅ | `grep` src + dist wxml 仅命中业务字典自然语义（会员=信鸽协会/VIP=转介绍活动/开通=地铁燃气），**UI 0 命中**，人工排除 |
| 6 | tabBar 仅 5 项 | ✅ | 首页/知识/策展/客户档案/我的（案例·测评降级为知识内非 tab 页） |
| 7 | 真渲染 e2e 功能走查 | ✅ | Playwright + 系统 Chrome：**21/21 全绿**（5 tab + 知识三模块 + 客户新建 + 策展全链路 + 认知卡） |
| 8 | 控制台/页面错误 = 0 | ✅ | 过滤 Uni 已知 scrollTop/favicon 噪音后：**0 错误** |
| 9 | 用户视角门禁 | ✅ | 5 tab + 知识三模块 + 我的页 **无会员/订阅/¥/积分角标**；首页信任积分未写死 150（B1 回归已修） |

## 五、微信审核红线（代码侧已清零 · 人工 3 项待办）

| 红线 | 结论 |
|------|------|
| 虚拟支付资质 | ✅ 不触发（功能码 0 命中，积分仅真实动作获得、不可购买/兑换） |
| 诱导分享 | ✅ 不触发（无「分享得积分」激励，转发仅为合法原生转发） |
| 绝对化用语 | ✅ 已修（最佳→精选） |
| 功能完整性 | ✅ 已修（去「模拟」占位 → 「后续版本开放」） |
| 隐私保护 | 🟢 已补 App 内隐私页；**MP 后台「隐私保护指引」需小鱼儿配置** |
| 类目资质 / ICP 备案 / 企业主体 | ⚠️ **需小鱼儿在 MP 后台确认**（代码无法代配） |

> 代码侧已消除全部可机器核验的驳回风险；剩余 3 项（隐私指引后台配置 / 类目资质 / 备案主体）为 MP 后台人工动作，提交微信审核前完成。

## 六、下一步（人工）

1. 小酒窝儿 P0 铁检通过 → 小鱼儿合并 PR #211
2. 微信开发者工具真机预览 → 上传 → 提交微信审核
3. MP 后台：配置隐私保护指引 + 确认房产中介类目资质 + ICP 备案/企业主体认证
4. 飞书群 `oc_68714b732f740d284e51268ba1b65614` 同步 PR #211 + 本工单结论

---

*自动送审 · 2026-08-01 · 关联 PR #211*
