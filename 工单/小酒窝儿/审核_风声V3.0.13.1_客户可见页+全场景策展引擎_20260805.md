> **状态**：🟠 待审
> **送审人**：小豆子
> **审核人**：小酒窝儿
> **日期**：2026-08-05
> **关联PR**：#211（feat/mp-v3-curation-prep-20260801，HEAD=4cc6188 起 V3.0.13.1 WIP；若 #211 已合并/关闭，请小扣子/小鱼儿基于该分支新开 PR）
> **优先级**：P0 紧急（2h 内 9 项铁检）
> **铁检项**：①图片/音频≤200K（0 超）②wxss 无中文类（0 文件）③无 `<small>`/`<br>` 等禁标签（0）④tabBar≤5（5 项：首页/知识/策展/客户档案/我的）⑤无 ¥/会员/VIP/订阅/充值/升级为企业（UI 层 0；仅 `entries_slim.js` 内容提及"会员费"为黑中介警示真实内容，按铁律人工排除）⑥libVersion 锁 2.32.3 ⑦真机预览 ⑧v-pay 泄漏扫描（0）⑨资源扫描（主包 1.7M / 分包 84K，单文件全 ≤200K）
> **微信合规自检（8 项）**：①虚拟支付 0 ②诱导分享 0（`incShare` 仅定义未接线、`earnTasks` 无 share/积分/红包）③绝对化用语 0 营销超级词（内容内"满五唯一/唯一住房"为真实房产术语，已人工排除）④占位信号 0（无"模拟/测试版/开发中"）⑤资源 ≤200K 0 超 ⑥wxss 中文类 0 / wxml 禁标签 0 ⑦主包 1.7M <2MB、分包 84K <2MB ⑧后端依赖 `login()` 仅本地登录态（`onLaunch` 无强制网络登录，核心功能纯本地）

# 风声 V3.0.13.1 —— 客户可见页（策展包对外交付）+ 全场景策展引擎收口

## 一、本次解决的问题

在 V3.0.5「真实词典 + 真实策展引擎」基础上，把策展产物从"内部沙盘"升级为"可交付给客户的见面包"，并补齐全场景覆盖：

### 1 · 客户可见页（curate-client，新增页面）
- 策展生成后点「客户可见页」进入 `package-curation/pages/curate-client`，把"说/带/问/见后跟进"渲染成客户视角的清爽版式。
- **数据诚实铁律落地**：因 `dataRef`/`caseRef` 当前覆盖率 0%，页面自动降级为版式示意，并明确提示「真实数据待小眼镜补入」，绝不编造数字/案例；无真实机制支撑处一律留"后续版本开放"。
- 支持「存入客户认知卡」（关联 clientId 时）、「摘要文本」「HTML 报告」「豆包提示词」四种导出，覆盖经纪人把方案变成交付物的全路径。

### 2 · 全场景策展引擎（engine.js + curate-prep）
- 4 条业务线全覆盖：购房（首套/改善/教育/升级/适老）、租住（起步/改善/家庭/品质）、业主出售、房东出租。
- 结果态新增 FABE 四维卡（功能/优势/利益/佐证）+ MOT 标签（按客户旅程节点标注），每条挂真实法源徽标（绝不编造）。
- 6 轮用户视角 Playwright 走查命中：购房改善 40 强相关/34 法源、业主出售 40/35、租住起步 40/27、购房教育 8/8、懒人默认 11/10 —— 绝大多数场景内容饱满、法源扎实。

### 3 · 埋点降级（tracker.js）
- `trackEvent` 在 H5 本地调试（127.0.0.1）跳过向 `fengsheng.tech/api/events` 的跨域请求，消除控制台 CORS 刷屏；mp-weixin 真机仍按 MP 后台合法域名静默上报，失败不影响主流程。

## 二、改动文件（6 个 + 新增 1 页）
- `src/package-curation/engine.js`：全场景引擎（+400 行，4 线节点/场景/打分）。
- `src/package-curation/pages/curate-prep/index.vue`：结果态 FABE+MOT+导出工具（+62 行）。
- `src/package-curation/pages/curate-client/index.vue`：新增客户可见页（真实/演示数据优雅降级）。
- `src/pages.json`：注册 curate-client 分包页。
- `src/pages/home/index.vue` / `src/utils/share.js` / `src/utils/tracker.js`：入口/版本号/埋点降级。

## 三、验证
- **构建**：`npm run build:mp-weixin` DONE（主包 1.7M / 分包 84K），`build:h5` DONE（版本号唯一 `3.0.13.1`）。
- **功能走查（用户视角 6 轮，H5+Playwright 真渲染）**：0 `PAGEERROR`、0 `console.error`；导出工具（客户页/HTML/豆包/摘要/存入认知卡）全通；无客户存档时「存入认知卡」诚实 toast 不假存。证据见 `fs-mini-program/outputs/ux_walk_v3.0.13.1_20260805/`。
- **铁检（见状态块）**：9 项全过或合规说明。
- **微信合规自检（8 项，见状态块）**：全过。
- ⚠️ **真机预览（微信开发者工具）仍为强制门槛**：沙箱只能构建+打包，最终由小扣子真机重传做终极证据（见人工待办）。

## 四、送审包
- `fs-mini-program/outputs/mp_weixin_v3.0.13.1_20260805.zip`（mp-weixin 构建产物）—— 请用此包真机重传预览。
- 分支 `feat/mp-v3-curation-prep-20260801`（V3.0.13.1 WIP，待 push origin 更新 PR #211）。

## 五、已知诚实缺口（非缺陷·已照实标注，不影响送审）
1. **房东出租线（R4 走查 0 命中）**：`entries_slim.js` 当前 landlord 数据为 0（与 main/分支 slim 双向分叉一致），该场景生成结果 say/bring/ask 为空、仅留 4 条见后跟进，页面诚实提示「该场景下暂无匹配词条」。属内容补全，已规划 slim union（main 704 ∪ 分支 634 = 768）作为送审后 #1 fast-follow，绝不臆造。
2. **dataRef / caseRef 覆盖率 0%**：客户可见页数据/案例区显示「待补充」，已诚实降级，不渲染占位假数据。
3. **隐私政策页 / 类目资质 / ICP 备案**：需 MP 后台人工配置（代码无法代配），提审前请小酒窝儿确认。

## 六、人工待办
1. **小扣子**：用 V3.0.13.1 包真机重传（重点验证：①策展→客户可见页渲染 ②tabBar 不遮挡自定义弹窗 ③scroll-view chip 不撑满 ④分包/主包体积）；真机跑通后上传并提交微信审核。
2. **小酒窝儿**：确认 MP 后台「隐私保护指引」「房产中介类目资质」「ICP 备案」「企业主体认证」已配；基于本工单触发内部审核结论。
3. **小豆子（fast-follow）**：① slim union 768 修房东出租线 ② dataRef/caseRef 按场景清单补真实数据（客户页清单已圈定，待小眼镜排产）③ 客户页占位隐藏渲染（隐藏而非显示"待补充"）。
