> **状态**：🟠 待审
> **送审人**：小豆子
> **审核人**：小酒窝儿
> **日期**：2026-08-03
> **关联PR**：#211（feat/mp-v3-curation-prep-20260801，分支 HEAD=e1e3214）
> **优先级**：P1 常规（12h 内审核）
> **铁检项**：①图片/音频≤200K（本次 0 超）②wxss 无中文类 ③无 `<small>`/`<br>` 等禁标签 ④tabBar≤5 ⑤无 ¥/会员/VIP/订阅/充值（虚拟支付） ⑥libVersion 锁 2.32.3 ⑦真机预览 ⑧v-pay 泄漏扫描 ⑨资源扫描

# 客户档案 V3.0.9.0 —— 迭代升级（运行监控埋点体系 · 让"正式上线后的数据"真正闭环）

## 一、本次迭代解决的问题（相对已上线 3.0.8.0）
3.0.8.0 已审核通过、正式运行，但**源码里没有行为埋点**——真实用户的使用数据（谁用了、用了哪块、策展几次）后端收不到。D1 + Worker 其实就绪（`events` 表 + `/api/events` 写入 + `/api/stats` 统计都有），`per_product` 里那 4 用户 / 80 PV 是测试残留。本版把"最后一公里"接通，让生哥要的「近一周数据同步 + 看板」真正有数据源。

## 二、改动内容
1. **新增埋点模块 `src/utils/tracker.js`**：匿名 uid（本地生成，不强制登录即可统计，隐私友好）；上报失败静默、绝不影响业务；复用既有 `/api/events`，`product='mini-program'`。
2. **业务事件埋点（store / 页面层）**：
   - `curate_generate` / `curate_save`：见面参谋**生成**与**沉淀到认知卡**（核心产品动作，漏斗最关键环节）
   - `client_add` / `client_update` / `client_remove`：客户档案增 / 改 / 删
   - `backup_export` / `backup_import`：备份导出 / 导入（数据掌控）
   - `knowledge_view` / `case_view`：业务词典词条 / 案例查看
   - `cognition_save` / `assessment_take`：认知卡沉淀 / 测评完成
3. **全局 pageview 上报**：`main.js` 全局 mixin，覆盖所有 tab 页（首页/知识/策展/客户档案/我的），带页面根组件去重。
4. **后端 `_worker.js`**：`/api/stats/daily` 增加 `product` 参数（支持 `?product=mini-program` 看小程序专属趋势，绑定参数防注入）。已提交 `4c40e64`，**待小扣子 `wrangler deploy` 生效**。
5. **管理看板 `dashboard.html`**：新增「📱 小程序·风声助手 · 运行看板」模块（累计用户/PV/点击/反馈 + 近 7 天趋势图），读 D1 实时数据。
6. **`share.js`**：`APP_VERSION` 升 `3.0.9.0`。

## 三、改动文件
- `fs-mini-program/src/utils/tracker.js`（新增）
- `fs-mini-program/src/main.js`：全局 pageview mixin
- `fs-mini-program/src/store/user.js`：addClient/updateClient/removeClient/mergeClients/saveCognition/addAssessment 埋点
- `fs-mini-program/src/package-curation/pages/curate-prep/index.vue`：gen()/save() 埋点
- `fs-mini-program/src/pages/clients/index.vue`：exportBackup 埋点
- `fs-mini-program/src/pages/knowledge/index.vue`：knowledge_view/case_view 埋点
- `fs-mini-program/src/utils/share.js`：`APP_VERSION` 升 `3.0.9.0`
- `_worker.js`（后端 product 维度，已合 `4c40e64`）、`dashboard.html`（看板模块）

## 四、机器可验合规自检（已本地跑通）
- ① 虚拟支付泄漏（.vue/.js 代码文案层）：合规已评估/VIP/充值/订阅/升级为企业 **0 命中**。业务字典 JSON 中"会员/开通"为正常房产释义词，不触发虚拟支付。
- ② 绝对化用语（国家级/顶级/最佳/唯一/首选）：仅命中"满五唯一"（房产专业术语，合法）+ 代码注释"唯一真源"，**营销绝对化 0 命中**。
- ③ 功能占位（模拟/测试版/开发中/敬请期待）：**0 命中**。
- ④ 资源 ≤200K：dist 全量扫描 **0 个超 200K**。
- ⑤ wxss 中文类选择器 / 禁标签（small/br/ul/li/i/b/span/label/h/p）：**0 命中**。
- ⑥ 版本号硬编码：wxml/js 残留 `3.0.x` 字面量除最终 `3.0.9.0` 外 **0 命中**。
- ⑦ libVersion（额外）：dist 产物锁 `2.32.3` ✅（已校验送审包内 project.config.json）。

## 五、验证（真渲染为证据）
- mp-weixin 重建 MP_EXIT=0，主包约 1.6MB < 2MB，资源 0 超 200K。
- `dist/build/mp-weixin/utils/tracker.js` 已编译进包，埋点代码生效。
- H5 + Playwright 走查无报错（pageview / 业务事件路径）。

## 六、送审包
- `outputs/mp-weixin-v3.0.9.0-20260803.zip`（774KB / 已校验 libVersion 2.32.3）—— 请用此包上传体验版真机复验。

## 七、人工待办（须团队/后台配合，代码层无法代配）
1. **小鱼儿：先合并 PR #211 → main**（关键前置！main 当前未含 V3.0.9.0 代码；工单触发审核机器人是从 main 拉代码，不合并则审到旧代码）。
2. **小扣子：`wrangler deploy`**（让后端 `/api/stats/daily?product=mini-program` 维度生效，看板近 7 天小程序趋势依赖它；代码已在 `4c40e64`）。
3. **小扣子：MP 后台「开发设置 → 服务器域名 → request 合法域名」加 `https://fengsheng.tech`**（⚠️ 不加则真机埋点上报被微信拦截，监控数据收不到）。
4. **小扣子：用 V3.0.9.0 包上传体验版**，真机走一遍「见面参谋生成 → 客户档案增改 → 导出/导入备份」验证埋点无报错；同时确认 MP 后台 request 域名已加。
5. **小酒窝儿：分级铁检**（P1，12h 内）。
6. **MP 微信公众平台后台**（须小鱼儿/小扣子登录，3.0.8.0 若已配可跳过）：隐私保护指引 / 房产中介类目资质 / 小程序 ICP 备案 / 企业主体认证。

> 备注：本工单须**直接提交 main** 才会触发审核机器人（与 V3.0.2~3.0.4 / V3.0.8.0 工单一致）。特性分支仅承载代码，工单不入特性分支。合并 PR #211 后正式送审，用户真机即开始累积真实运行数据，生哥的「每周一数据同步」自动化即生效。
