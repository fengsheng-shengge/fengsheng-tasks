# 审核工单：#222 P0 177 条 dataRef/caseRef 真值回填

- **提交方**：小眼镜
- **审核人**：小酒窝儿
- **提交日期**：2026-08-24 17:55
- **工单优先级**：P0（时限 30min，自提交起计）
- **数据口径**：真源 main 实时 6968 条（dataRef/caseRef 为占位，非字段缺失）

## 一、交付物清单（本目录下）

| 文件 | 说明 |
|------|------|
| `#222_P0_177_dataRef_caseRef_产出.json` | 177 条 P0 词条的真值产出（dataRef/caseRef 对象结构） |
| `#222_P0_dataRef_caseRef_审核单_20260824.html` | 审核工作底稿：177 条逐条差异清单（原占位→新值），支持 entryType 筛选/搜索/指标卡，可直接打开 |
| `#222_补全后_entries.json` | 落库交付物：真源 6968 条全量，177 条 dataRef/caseRef 已替换为对象，其余字段未动 |

## 二、数据契约（交接文档第三节，小豆子确认 engine 已加取值适配）

- **dataRef 对象**：`{value, unit, asOf, source}`；value 禁"待补充/待核/无"占位；source 必填
- **caseRef 对象**：`{desc, caseNo, source}`；caseNo 可空；source 必填
- **tags.clientType 铁律**：未改动原字段
- **数据诚实铁律**：无真实出处不编造，caseRef 仅 19 条 detail 含真实判例/执法案例者填写，其余 158 条无真实案例故留空（前端诚实留白）

## 三、产出统计

- 唯一词条：177 条（P0 全量）
- dataRef 回填：177/177（100%，≥60% 目标达成）
- caseRef 回填：19/177（真实判例/执法案例）
- 占位/空值：0
- 未匹配真源：0

## 四、验收要求

1. 按审核单 HTML 逐条核对 dataRef 的 value 是否有真实来源支撑（source 字段可核验）
2. 抽查 caseRef 19 条的判例真实性
3. 确认补全后_entries.json 与真源 6968 条口径一致、无字段误改
4. 审核结论回填多维表《风声·审核与工作交接留痕库》对应记录

审核通过后：由小酒窝儿确认 → 合入 origin/main data/entries.json → 后端重载 → API 生效。
