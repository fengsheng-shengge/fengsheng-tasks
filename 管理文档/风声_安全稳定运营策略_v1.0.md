# 风声 · 安全稳定运营策略 v1.0

> **CTO · 小扣子 | 2026.06.22**

---

## 一、策略总览

网站是风声运营核心目标：**全年99.9% 可用性，安全零事故**。分为四套机制：

| 机制 | 目标 | 负责 |
|------|------|------|
| 🔴 安全防护 | 防DDoS·防注入·防爬 · 防XSS·防CSRF | WAF + 代码层 |
| 🟡 每日巡检 | 自动化检查·异常告警 | Python脚本 + GitHub Actions |
| 🟢 监控告警 | 页面宕机秒级发现·5分钟响应 | 健康检查API + 邮件告警 |
| 🔵 应急响应 | 故障分级·5分钟切换到恢复·1小时恢复 | Runbook + 一键回滚 |

---

## 二、安全防护层 · 四道防线

### 防线1：Cloudflare WAF（网络层）
Cloudflare Pages 内置 WAF + Bot Fight Mode，自动拦截 DDoS/恶意爬虫。

| 规则 | 动作 | 备注 |
|------|------|------|
| 自动识别机器人流量 | Block | Cloudflare 内置 |
| 国家/地区限制 | Log only | 中国境内部署在 Cloudflare Dashboard 配置 |
| 速率限制 | Challenge | 单个IP 300次/分钟触发验证码 |
| SQL 注入/XSS | Managed Ruleset | Cloudflare 托管规则集 |

### 防线2：HTTP Security Headers（应用层）

```
Strict-Transport-Security: max-age=63072000; includeSubDomains
X-Content-Type-Options: nosniff
X-Frame-Options: SAMEORIGIN
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: geolocation=(), microphone=(), camera=()
Content-Security-Policy: default-src 'self' data:; img-src 'self' data: https:; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; connect-src 'self' https:; font-src 'self' data:
```

### 防线3：API 限流 & 输入校验（代码层）

```javascript
// 单IP 60秒内 60次请求
const RATE_WINDOW = 60_000;
const RATE_MAX_PER_IP = 60;

// 统一输入过滤
function sanitize(str) { return str?.slice(0, 2000).replace(/[<>]/g, ''); }
```

### 防线4：CSRF 校验（代码层）
- 所有 POST 请求校验 Origin / Referer
- 同源策略：仅 fengsheng.tech 允许

---

## 三、每日巡检体系 · 四层检查

**巡检频率**：北京时间 09:00 / 14:00 / 22:00
**执行方式**：GitHub Actions 定时触发 + Python 脚本

### 巡检清单（20项·全覆盖）

| 层级 | 检查项 | 阈值 | 告警级别 |
|------|--------|------|---------|
| 🟢 页面层 | 首页200 | < 3秒 | 正常 / 警告 / 严重 |
| | 知识底座200 | < 3秒 | 正常 / 警告 / 严重 |
| | 品质测评200 | < 3秒 | 正常 / 警告 / 严重 |
| | 关怀测评200 | < 3秒 | 正常 / 警告 / 严重 |
| | 培养师200 | < 3秒 | 正常 / 警告 / 严重 |
| | 客户解码200 | < 3秒 | 正常 / 警告 / 严重 |
| | 带看报告200 | < 3秒 | 正常 / 警告 / 严重 |
| | 管理看板200 | < 3秒 | 正常 / 警告 / 严重 |
| 🟢 API层 | /api/ping 200 | < 1秒 | 严重 |
| | /api/stats/health 200 | < 1秒 | 严重 |
| | /api/feedback POST 响应正常 | < 2秒 | 严重 |
| | /api/event POST 响应正常 | < 2秒 | 严重 |
| 🟢 数据层 | D1 连接正常 | API 返回 ok | 严重 |
| | events表 > 0 | 有数据 | 警告 |
| | feedback表 > 0 | 有数据 | 警告 |
| 🟢 基础设施 | DNS 解析正常 | < 100ms | 警告 |
| | SSL 证书有效期 | > 30天 | 警告 / 严重 |
| | sitemap.xml 有效 | 含18 URL | 警告 |
| | _redirects 全通 | 7条规则 | 警告 |
| | og:image 全页面覆盖 | 25个文件 | 警告 |

---

## 四、监控告警体系 · 三级响应

### 告警级别定义

| 级别 | 触发条件 | 响应方式 | 响应时间 |
|------|---------|---------|---------|
| 🔴 P0 严重 | 页面5xx / API 全挂 / D1断开 | GitHub Issue + 邮件 + 推送 | 5分钟 |
| 🟡 P1 警告 | 响应>3秒 / 数据异常 / og:image缺失 | GitHub Issue | 1小时 |
| 🟢 P2 提示 | 数据流入正常·无异常 | 日志记录 | 24小时 |

### 告警渠道
1. **GitHub Issues 自动开单**：检测到异常自动生成工单、自动认领
2. **邮件告警**（待配置邮件通知配置）

---

## 五、应急响应 Runbook

### P0 级：网站宕机 / API 挂掉

```
1. 打开 Cloudflare Dashboard → Pages → 部署历史 → 选择最近成功commit → 一键回滚
2. 检查 GitHub Actions → Deploy 日志 → 确认是否是哪个环节出错
3. 确认错误日志：tail 错误内容
4. 本地复现
5. 修复提交 → 部署
6. 通知全员：生哥/小鱼儿
```

### P1 级：页面响应慢 / 数据异常

```
1. 检查 API /api/stats/summary
2. 查看 7日趋势图
3. 查看 数据库是否异常
4. 查看反馈表是否有大量反馈
5. 诊断问题
6. 修复
```

### P2 级：og:image/meta标签缺失

```
1. 运行脚本补全
2. Commit 部署
```

---

## 六、版本控制 & 回滚策略

| 操作 | 命令 | 说明 |
|------|------|------|
| 🚀 部署 | git push origin main | 自动触发 Cloudflare Pages 部署 |
| ⏪ 回滚 | git revert <hash> | 回滚到指定commit |
| 🕒 查看 | git log --oneline -5 | 查看最近5个commit |
| 🔒 Force Push | **禁止！** | 会覆盖他人提交 |

### 部署前 Checklist
1. [ ] 本地启动测试无语法错误
2. [ ] Python 巡检脚本本地跑一遍无 red
3. [ ] 3个核心页面手工验证
4. [ ] Push 到 GitHub

---

## 七、数据备份策略

- **D1数据库**：每周日 03:00 自动备份（Cloudflare自动备份）
- **代码库**：GitHub 自动版本控制 + 本地 clone
- **配置文件**：wrangler.toml / _headers / _redirects 随代码入库

---

## 八、CTO 每日5分钟检查清单

```
🟢 09:00 自动巡检报告 → 查看邮件/GitHub Issue
🟢 14:00 人工抽查3个核心页面
🟢 22:00 夜间巡检
```

---

*v1.0 · 小扣子（CTO）· 2026.06.22*
